// Entry point for frontend logic

document.addEventListener("DOMContentLoaded", () => {
  document.getElementById("app").innerHTML = `
    <h1>Investment Memo Analyzer</h1>
    <p>Upload a memo or paste text to begin.</p>
    <!-- Add UI elements here -->
  `;
});