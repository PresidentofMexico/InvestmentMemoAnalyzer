// Optional client-side override for API base URL.
// In environments where the frontend is served separately from the API server,
// set window.API_BASE_URL to the API’s absolute URL, e.g.:
// window.API_BASE_URL = 'https://your-backend.onrender.com';
// By default, this remains undefined and the app will auto-detect.

// Lock API to your Render Web Service URL
window.API_BASE_URL = 'https://investmentmemoanalyzer.onrender.com';
